# Skills

本目录存放学习工作流使用的 skill 规则、意图说明与模板。

## 目录约定

- `shared/`: 跨意图复用的通用规则
- `intents/`: 与 router intent 一一对应的执行说明
- `templates/`: 生成内容时优先复用的模板

## 兼容说明

仓库中已有的历史 skill 文件暂时保留，不强制迁移。
后续新增内容优先放入上述三层结构中，逐步完成整理。
