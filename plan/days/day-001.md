# day-001 学习计划

> day-first workflow：今天只学习 `outline-5-1`，输出落到 `study/day-001/`。

## Day

- 编号：`day-001`
- 计划文件：`plan/days/day-001.md`
- 学习目录：`study/day-001`
- 今日主题：数据类型、变量绑定、`None`、布尔值和最小输出表达

## 固定大纲引用

- `outline-5-1`

## 学习资料来源

- `source/《Python基础教程第3版》.pdf`
- `source/liaoxuefeng-python-introduction-from-basic/pages/001-books-python-basic-index-html.json`
- `tmp/pdfs/《Python基础教程第3版》.json`

## 今日目标

- 建立 Python 5 个基础值类型的最小直觉：`int`、`float`、`str`、`bool`、`None`
- 理解“变量是名字绑定到值”，而不是 JS 里的 `let/const` 翻版
- 会用 `type()`、`print()`、f-string 做最小调试输出
- 能修掉最常见的“字符串和数字直接拼接”错误

## 今日安排

1. 阅读 `study/day-001/lesson.md`，先看概念和 3 个示例，控制在 `25-30` 分钟
2. 打开 `study/day-001/practice.py`，先直接运行一遍，再完成 5 组任务，控制在 `40-55` 分钟
3. 对照 `study/day-001/answers.md` 自查，重点核对 `None`、变量绑定、f-string，控制在 `15-20` 分钟
4. 在 `study/day-001/notes.md` 和 `study/day-001/mistakes.md` 记录结果，控制在 `10-15` 分钟

## 今日最小产出

- 能解释 `None` 和 `False` 不是一回事
- 能解释为什么 `a = "ABC"; b = a; a = "XYZ"` 后 `b` 仍然是 `"ABC"`
- 能把数字、字符串、布尔值放进同一条 f-string 输出
- 完成 `practice.py` 中至少 5 组任务

## 完成判断

- 你能不看答案说出 5 个基础值类型分别长什么样
- 你能读懂 `type(value)` 的输出
- 你能自己修掉“字符串 + 数字”报错
