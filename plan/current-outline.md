# Current Dynamic Outline

> 说明：这是当前动态学习大纲的人类可读视图。唯一真实数据源是 `state/dynamic-outline.json`。

## 当前主线

- 当前 day：`day-001`
- 当前 day 关联 unit：`Python基础 / 5.1-数据类型和变量`
- 当前阶段：Python 核心语法与数据结构
- 当前节点：变量、条件、循环、列表、字典
- 当前状态：day-001 计划和任务已生成，等待学习结果回写

## 动态调度原则

- 学习任务不能只按固定周次生成，必须同时参考当前主线节点、前置依赖状态和薄弱点。
- 如果一次学习结果不理想，不直接机械推进后续主题，而是按严重度插入复习块或补强块。
- 只有当当前节点达到“稳定”后，才继续推进下一个主线节点。

## 当前阶段节点

### 1. 变量、条件、循环、列表、字典

- 目标：建立从 JS/TS 迁移到 Python 的基础表达能力
- 当前判断：初始化完成，进度为 0，下一步从环境、解释器和第一个 Python 脚本开始
- 下次任务生成时必须考虑：
  - 先建立 Python 运行方式和最小脚本直觉
  - 再推进变量、字符串和基础数据结构

### 2. 函数、参数、返回值

- 状态：排队中
- 前置要求：
  - 基础语法和常见数据结构已能独立使用
  - 能看懂并运行最小 Python 脚本

## 当前补强队列

当前为空。项目已初始化，尚未产生历史薄弱点。

## 学习结果如何影响后续任务

- `good`：正常推进主线，在练习里保留轻量复习
- `mixed`：下次任务前增加补强块，并在练习里增加边界题
- `poor`：暂停推进后续主线，先单独补前置知识
