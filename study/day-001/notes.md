# day-001 notes

## 今天确认的知识点

- `int / float / str / bool / None` 分别对应什么值
- `None` 表示“没有值”，不是 `False`
- Python 变量是名字绑定到值，不需要 `let` / `const`
- 混合类型输出优先用 f-string

## 我自己的表述

- 用你自己的话解释：什么叫“变量绑定”
- 用你自己的话解释：`None` 和 `False` 差在哪里

## JS / TS 对照

- 今天最像 JS / TS 的点：
- 今天最容易被 JS / TS 误导的点：

## 还不稳定的地方

- 

## 明天前需要记住的一句

- 
